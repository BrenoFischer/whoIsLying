import { moderateScale } from "react-native-size-matters";

export const fontSize = {
    sm: moderateScale(12),
    md: moderateScale(16),
    lg: moderateScale(20),
    xl: moderateScale(28),
    xxl: moderateScale(34),
};